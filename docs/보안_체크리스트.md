# 보안 체크리스트 대조표

> 과제 제공 보안 체크리스트를 실제 구현·검증(테스트)과 1:1 대조한 표.
> 상태: ✅ 충족 · ⚠️ 부분 충족 · 🟡 배포 영역 · ⬜ 미흡
> 검증 열의 `SEC/TC`는 `docs/REPORT.md` §5, 코드 위치는 해당 파일 참조.

## 요약

| 섹션 | 항목 수 | ✅ | ⚠️ | 🟡 |
|------|:---:|:---:|:---:|:---:|
| 회원가입 및 프로필 관리 | 7 | 7 | 0 | 0 |
| 상품 등록 및 관리 | 5 | 5 | 0 | 0 |
| 실시간 채팅 및 메시징 | 5 | 5 | 0 | 0 |
| 안전 거래 및 신고 | 4 | 4 | 0 | 0 |
| **합계** | **21** | **21** | **0** | **0** |

- **자동화 테스트: 163/163 통과** (기능 스모크 + `tests/test_security.py` 16종)
- **21개 항목 전부 충족 ✅** (WSS는 운영/`USE_TLS`에서 wss로 검증)

---

## 1. 회원가입 및 프로필 관리

| # | 체크리스트 항목 | 상태 | 구현 | 검증 |
|---|-----------------|:---:|------|------|
| 1 | 서버측 입력 검증 | ✅ | `auth.py` WTForms `Length`/`Regexp`(아이디 3~20 영숫자_), 비번 8자+ · 출력 Jinja 이스케이프 | 사용자명 특수문자 거부(test_security), TC-03/04 |
| 2 | CSRF 보호 | ✅ | `app.py` `CSRFProtect` 전 POST 폼 | SEC-05, **SEC-22**(토큰 없는 POST→400) |
| 3 | 비밀번호 보안 | ✅ | `models.set_password` werkzeug **scrypt + 계정별 salt**(원문 미저장) | §3.3 #1 · DB 덤프 근거 |
| 4 | 세션 쿠키 설정 | ✅ | `config.py` HttpOnly·SameSite=Lax·Secure(배포 env) | **SEC-23** |
| 5 | 세션 만료 및 재인증 | ✅ | 세션 만료 1h(`PERMANENT_SESSION_LIFETIME`, 유휴 갱신) + **비번 변경 시 현재 비밀번호 재인증** | SEC-24(고정), TC-63(재인증) |
| 6 | 실패 로그인 방어 | ✅ | `auth.py` (IP+아이디) 실패 5회/5분 쿨다운 | **SEC-19**, TC-60 |
| 7 | 오류 메시지 | ✅ | `app.py` 커스텀 403/404/500 + 배포 `DEBUG=False` | **SEC-20**, TC-61 |

## 2. 상품 등록 및 관리

| # | 체크리스트 항목 | 상태 | 구현 | 검증 |
|---|-----------------|:---:|------|------|
| 8 | 폼 입력 검증 | ✅ | `product.py` 제목/설명 길이, 가격 `NumberRange(0~10억)` | **SEC-26**(음수·비숫자 거부), TC-10 |
| 9 | XSS 방어 | ✅ | 상품 설명 Jinja 자동 이스케이프(`\|safe` 미사용) | **SEC-27**, SEC-01 |
| 10 | 인증된 사용자만 등록 | ✅ | `@login_required`(new/edit/delete/mine) | TC-13 |
| 11 | 소유자 확인 | ✅ | `_get_owned_product_or_403`(소유자/관리자만) | **SEC-03** |
| 12 | 데이터 무결성 | ✅ | WTForms 검증 + 모델 `nullable=False` | TC-10 |

## 3. 실시간 채팅 및 메시징

| # | 체크리스트 항목 | 상태 | 구현 | 검증 |
|---|-----------------|:---:|------|------|
| 13 | 메시지 내용 검증 | ✅ | `chat.on_send` 길이 1000·빈값 무시 · 클라 `textContent` | **SEC-10** |
| 14 | 사용자 인증 | ✅ | `connect`에서 세션 검증(없으면 거부) | **SEC-08** |
| 15 | 메시지 검증 | ✅ | 서버측 방 참여자·내용 검증 | **SEC-09** |
| 16 | Rate Limiting | ✅ | 사용자별 3초당 5건 초과 차단(스팸 방지) | **SEC-21**, TC-62 |
| 17 | 연결 암호화(WSS) | ✅ | `io()` 동일 오리진이라 https 페이지에서 자동 `wss`. `USE_TLS=1`(자체서명) 또는 운영 리버스프록시 TLS로 종단 | **wss:// 확인**(DevTools) + §6.3 |

> ⑰ 검증: `USE_TLS=1 python app.py` → `https://localhost:5000` → DevTools Network의 WS가 `wss://`. (기본 개발 실행은 `ws`, 운영/USE_TLS는 `wss`)

## 4. 안전 거래 및 신고

| # | 체크리스트 항목 | 상태 | 구현 | 검증 |
|---|-----------------|:---:|------|------|
| 18 | 폼 입력 검증 | ✅ | `report.py` `target_type` 화이트리스트·`target_id` 정수·사유 max500 | **SEC-13**, 사유길이(test_security) |
| 19 | 인증된 사용자 접근 | ✅ | `@login_required` | test_report |
| 20 | 데이터 무결성 및 로그 관리 | ✅ | `reports` 형식 저장·기록 + **관리자 감사로그**(`AdminLog`, `/admin/logs`) 기록·조회 | TC-64 |
| 21 | 신고 남용 방지 | ✅ | 중복신고 UNIQUE·자기신고 금지·임계치5·관리자 검토(이의제기) | **SEC-11/12**, TC-30 |


---

## 부록. 체크리스트 외 추가 보안 조치 (가산)

과제 체크리스트에 없지만 구현·검증한 항목:

| 조치 | 내용 | 검증 |
|------|------|------|
| 파일 업로드 방어 | 확장자 화이트리스트 + **매직바이트** 검증 + 난수 파일명 + 5MB 제한 | SEC-04 |
| 경로조작 차단 | `send_from_directory` + `../` 접근 404 | **SEC-25** |
| 관리자 권한 분리 | `admin_required` 전 라우트(비관리자 403), 자기 잠금/삭제 방지 | SEC-14 |
| 이의제기 인증 분리 | 휴면 우회 로그인 차단(세션 미발급) | SEC-15 |
| 가상 지갑 트랜잭션 무결성 | 조건부 UPDATE(경쟁조건/이중지불 방어) + `CHECK(balance>=0)` | SEC-16/17 |
| 사용자 열거 방지 | 로그인 실패 동일 메시지 | SEC-02 |
| 외부 CDN 제거 | Socket.IO 자체 호스팅(공급망) | — |

---

_참고: 위 21개 항목은 제공 이미지(4개 섹션) 기준이다. 원본 체크리스트에 추가 항목이 있으면 이 표에 이어서 반영한다._
